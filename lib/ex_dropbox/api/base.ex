defmodule ExDropbox.Api.Base do
  @moduledoc """
    Base module that acts as a mediator for the api resources
  """
  def get do
    IO.puts "should build an url and make the actual request to dropbox"
  end

  def post do
    IO.puts "should build an url and make the actual request to dropbox"
  end
end
